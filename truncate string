function truncate(str, num) {
  var ellipse = "...";
  if(str.length > num)
    {
      var newStr = str.slice(0,num-ellipse.length) + "...";
    }
  else
    {
      return str;
    }
  return newStr;
}

truncate("A-tisket a-tasket A green and yellow basket", 11, "");
